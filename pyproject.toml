[project]
name = "g1-bobby"
version = "0.1.0"
description = "Mock-first teleoperation spine for Unitree G1 development."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0,<1.0.0",
    "pydantic>=2.8.0,<3.0.0",
    "pydantic-settings>=2.4.0,<3.0.0",
    "uvicorn[standard]>=0.30.0,<1.0.0",
    "websockets>=10.4,<17.0",
]

[project.scripts]
g1-bobby-operator = "g1_bobby_operator_cli.main:main"
g1-bobby-unitree-probe = "g1_bobby_unitree_bridge.main:main"
g1-bobby-unitree-listen = "g1_bobby_unitree_bridge.listener:main"
g1-bobby-unitree-sim = "g1_bobby_unitree_bridge.sim:main"

[build-system]
requires = ["setuptools>=75.0.0"]
build-backend = "setuptools.build_meta"

[project.optional-dependencies]
dev = [
    "httpx>=0.27.0,<1.0.0",
    "pytest>=8.3.0,<9.0.0",
    "pytest-asyncio>=0.24.0,<1.0.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = [
    "apps/api",
    "apps/operator_cli",
    "apps/unitree_bridge",
    "packages/contracts",
    "packages/safety",
    "packages/robot_adapters",
]
testpaths = ["tests"]

[tool.ruff]
line-length = 100

[tool.setuptools.packages.find]
where = [
    "apps/api",
    "apps/operator_cli",
    "apps/unitree_bridge",
    "packages/contracts",
    "packages/safety",
    "packages/robot_adapters",
]
